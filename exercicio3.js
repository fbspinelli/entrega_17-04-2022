//3) Crie um objeto JSON que abriga todas as operações do exercício 2


json = {
    soma : (x, y) => {return x + y},
    subtracao : (x, y) => {return x - y},
    multiplicacao : (x, y) => {return x * y},
    divisao : (x, y) => {return x / y}
}
